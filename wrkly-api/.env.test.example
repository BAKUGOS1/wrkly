# Copy this to .env.test and fill in your local Postgres credentials.
# Tests run against a REAL database and clean up after themselves.
# Use a dedicated test database (NOT production)!

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/wrkly_test"
JWT_SECRET="test-secret"
NODE_ENV="test"
